# Paper Tasks

- Replace the smoke-test dataset with real minute-level ETF data for `SPY`, `QQQ`, and `VXX`.
- Add source-backed citations and a bibliography file.
- Insert pooled and per-symbol model comparison tables from `outputs/tables/`.
- Add at least one figure showing early-warning behavior around a held-out event.
- Add a sensitivity table for label threshold, feature window, and embedding parameters.
- Decide whether to add a formal econometric baseline beyond the rolling-volatility baseline.
